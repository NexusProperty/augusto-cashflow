#!/bin/sh
# Phase 4.6 backfilled this hook from Mission Control's scaffold baseline.
# Lint is intentionally NOT in this hook because augusto-cashflow's ESLint config
# pre-dates flat-config conventions and `eslint .` ignores everything (separate
# follow-up to fix). When the lint config is fixed, add `node node_modules/eslint/bin/eslint.js .`
# back to this hook above the typecheck line.
node node_modules/typescript/bin/tsc --noEmit
