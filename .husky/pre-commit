# Phase 4.6 backfilled this hook from Mission Control's scaffold baseline.
# Lint is intentionally NOT in this hook because augusto-cashflow has 72
# pre-existing lint errors (surfaced after Phase-4.6 follow-up fixed the
# previously-broken ESLint flat-config). When the source-cleanup sprint
# lands, add the lint line back above typecheck:
#   node node_modules/eslint/bin/eslint.js .
node node_modules/typescript/bin/tsc --noEmit
